@model Service
@{
    ViewData["Title"] = "Sua Dich Vu";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark">
                    <h4 class="mb-0"><i class="bi bi-pencil"></i> Sua Dich Vu</h4>
                </div>
                <div class="card-body">
                    <form method="post">
                        <input type="hidden" asp-for="ServiceId" />

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label">Ten Dich Vu <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" required />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Type" class="form-label">Loai Dich Vu <span class="text-danger">*</span></label>
                            <select asp-for="Type" class="form-select" required>
                                <option value="">-- Chon loai dich vu --</option>
                                <option value="LAB">Xet nghiem</option>
                                <option value="IMAGING">Chan doan hinh anh</option>
                                <option value="EXAM">Kham</option>
                            </select>
                            <span asp-validation-for="Type" class="text-danger"></span>
                            <small class="text-muted">Hien tai: <strong>@Model.Type</strong></small>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Price" class="form-label">Gia (VND) <span class="text-danger">*</span></label>
                            <input asp-for="Price" type="number" step="1000" class="form-control" required />
                            <span asp-validation-for="Price" class="text-danger"></span>
                            <small class="text-muted">Gia tinh bang VND, nen la boi so cua 1000</small>
                        </div>

                        <div class="alert alert-warning">
                            <i class="bi bi-exclamation-triangle"></i> 
                            <strong>Canh bao:</strong> Thay doi gia dich vu se anh huong den cac don hang moi
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a href="@Url.Action("Index")" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Quay Lai
                            </a>
                            <button type="submit" class="btn btn-warning">
                                <i class="bi bi-save"></i> Cap Nhat
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card shadow-sm mt-3">
                <div class="card-body">
                    <h6><i class="bi bi-info-circle"></i> Thong tin them</h6>
                    <p class="mb-1"><strong>ID Dich Vu:</strong> @Model.ServiceId</p>
                    <p class="mb-0"><strong>Loai:</strong> 
                        @if (Model.Type == "LAB")
                        {
                            <span class="badge bg-info">Xet nghiem</span>
                        }
                        else if (Model.Type == "IMAGING")
                        {
                            <span class="badge bg-warning text-dark">Chan doan hinh anh</span>
                        }
                        else if (Model.Type == "EXAM")
                        {
                            <span class="badge bg-primary">Kham</span>
                        }
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
